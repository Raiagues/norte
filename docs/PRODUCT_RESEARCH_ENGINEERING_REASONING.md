# Engineering reasoning: product research

Research date: 8 September 2026. The latest product specification supersedes the earlier generation button, permanent drawer and formal change form. These are product decisions informed by the primary sources below; they are not claims of measured usability or engineering accuracy.

## Relevant patterns

| Reference | Problem and interaction | Why it helps; application to Norte | Do not copy |
| --- | --- | --- | --- |
| [FigJam AI](https://help.figma.com/hc/en-us/articles/16822138920343-Use-AI-tools-in-FigJam), [board generation](https://help.figma.com/hc/en-us/articles/18706554628119-Make-boards-and-diagrams-with-FigJam-AI) | Turns a short prompt into visual material; selected text supplies action context. | Existing writing can become the starting point for assistance. Keep the empty canvas invitation and act on an existing technical hypothesis. | General brainstorming prompts, sticky clustering, rewriting, Jambot and a prominent AI destination. |
| [Miro object AI](https://help.miro.com/hc/en-us/articles/10339593336210-Using-Miro-AI-with-board-objects), [keyboard navigation](https://help.miro.com/hc/en-us/articles/11997028019858-Keyboard-navigation-while-working-on-boards) | An object selection exposes a menu above that object; its actions are keyboard accessible. | The action stays beside the work and only occupies space when relevant. Offer one contextual impact action on a recognized hypothesis; retain keyboard access and visible focus. | The broad creation toolbar, generation options, clustering and commands without engineering context. |
| [Onshape Where Used](https://cad.onshape.com/help/Content/Document/where_used.htm) | Starts from a selected part, finds the assemblies/drawings using its version, then filters and drills into results before a change. | Begin with the actual baseline object, then reveal its relevant neighborhood. Stable entity IDs and baseline revisions prevent ambiguity. | A complete product database browser or version-management interface. |
| [Autodesk Fusion change orders](https://help.autodesk.com/view/fusion360/ENU/?contextId=MNG-COS-OVERVIEW), [Fusion Manage panel](https://help.autodesk.com/cloudhelp/2026/ENU/Vault-General/files/VAULT-FM-PANEL-VIEW.htm) | A change associates affected items and attachments; product structure expands by levels and breadcrumbs retain navigation context. | Preserve the proposed change, affected objects and evidence together. Show macro architecture first, then subsystem contents with a breadcrumb. | Formal approvers, release workflow, long change forms, persistent multi-tab panels. A meeting hypothesis is an analysis, not a released revision. |
| [PTC Windchill digital product traceability](https://support.ptc.com/help/windchill/r13.1.2.0/pt_BR/Windchill_Help_Center/reqmgmt/RMDPT.html), [affected objects](https://support.ptc.com/help/windchill_rest_services/r2.7/en/windchill_rest_services/example_changemgmt_CreateChangeTaskwithAffectedorResultingObjects.html) | Connects independently managed engineering objects and identifies the existing objects affected by a change. | Keep requirements, properties, evidence and typed relations independently addressable. Traverse actual links instead of treating textual or spatial similarity as physical causation. | Enterprise federation, configuration administration and a large ontology. |
| [Siemens Teamcenter requirements](https://blogs.sw.siemens.com/teamcenter/requirements-management-process/), [engineering change workflow](https://blogs.sw.siemens.com/teamcenter/engineering-change-workflow/) | Relates requirements to supporting specifications and documents; identifies affected designs and requirements before approval. | A requirement should reveal what it constrains and where its statement originated. Keep it in an independent list that highlights the relevant architecture. | Approval queues, dashboards, mandatory workflow roles and reporting machinery. |
| [Dassault CATIA systems validation](https://www.3ds.com/products/catia/systems-engineering/systems-validation) | Maintains coverage and modification traceability across requirements, architecture, implementation and validation. | Show the transitions that connect a conclusion to its requirements and source facts. Missing validation remains visible as review work. | Certification claims, heavyweight V&V management and representing every trace object as a visible node. |
| [NASA configuration management](https://www.nasa.gov/reference/6-5-configuration-management/), [system design processes](https://www.nasa.gov/reference/4-0-system-design-processes/) | Keeps a known baseline and reviews alternative designs with data, analyses and uncertainties before recording a decision. | Persist the extracted model; keep scenarios separate. Expose fact, deterministic calculation and inference records rather than an opaque conclusion. Progress is a project record; visiting an earlier screen does not reverse it. | NASA-specific approval gates or any suggestion that Norte certifies a design. |
| [ESA Concurrent Design Facility](https://www.esa.int/content/view/full/433795) | Specialists work together on a shared design model that evolves during multidisciplinary sessions. | Open the shared System immediately after reading Project Memory; a visible local impact path supports live discussion across disciplines. | Space-only taxonomy or an entire concurrent engineering platform. |

## Discovery alternatives

Interaction counts below are design estimates from an empty workspace, not measured results. All approaches require the same explicit scenario analysis and never update the baseline automatically.

| Approach | Interactions | Learnability / footprint | Meeting speed / ambiguity | Baseline connection |
| --- | --- | --- | --- | --- |
| **Write a hypothesis on a card → contextual impact action → confirm recognized target/value** | Write + action + confirm | Keeps the existing click-to-write behavior; one local action | Fast for “Payload 280 g”; recognition can be ambiguous, so show the mapping before analysis | Explicit entity/property preview; only offer the action for a usable match |
| Select a system entity → what-if → edit value | Select + action + edit + run | Predictable but requires finding the object first | Precise and fast when already inspecting System | Strong: starts at a known entity |
| Global natural-language command palette | Open + write + resolve + run | Small when closed, but the command must be discovered | Flexible; hidden commands and unrestricted interpretation increase ambiguity | Requires a separate target-resolution step |

**Choose the contextual card hypothesis for Discovery.** It makes writing useful before demanding system navigation. In System, use the same scenario editor from the selected entity, already resolved; this is a second entry point to one interaction, not another change workflow. Restrict initial recognition to supported entity/property/value patterns. Unrelated notes remain quiet. Component alternatives with missing specifications produce a localized missing-data question, never invented numbers.

## Provenance and focus

Use two levels of disclosure: a compact node with an explicit information affordance, then a focused technical dialog containing values, dependencies and source excerpts. The dialog retains the graph behind it and restores focus when closed; its bounded width also supports smaller screens. Clicking a value's source or a relation exposes its evidence inside this context. Hover may explain an icon but cannot be the only access path. Keep excerpts short, highlight the cited text, and show a locator only when actually known.

The graph remains full width when information is closed. Clicking a subsystem focuses its children; a breadcrumb returns to the parent. Zoom navigates the canvas and does not unexpectedly change the architecture. During analysis, show only the relevant path and immediate supporting context, with labels as well as color. Requirements remain outside normal architecture nodes; their overlay can dim unrelated entities and highlight linked edges without losing the graph position.

## Production URL investigation

The repository identifies `https://raiagues.github.io/norte/` as its frontend demo. `render.yaml` configures a service and `/api/health`, but contains no production URL. A read-only public GitHub API check of repository metadata, the latest 100 deployments, commit statuses and check runs exposed only the `github-pages` environment and GitHub Actions links. Public searches did not identify a verified Render application. The Render service name is not evidence of its hostname. No production URL was guessed or deployment changed; README must explicitly retain this verification limitation until a canonical URL is available.

## Document-processing scope

Google's [Generate Content document guide](https://ai.google.dev/gemini-api/docs/generate-content/document-processing) documents inline PDF content for the endpoint already used by Norte. It describes other document inputs as text, without reliable preservation of visual structure. Norte can decode UTF-8 text/Markdown/CSV/JSON/code itself and provide bounded PDF data to Gemini. This does not establish reliable DOCX/XLSX parsing in the current integration; keep those explicitly unparsed and ask for PDF/CSV/text exports. The [structured-output guide](https://ai.google.dev/gemini-api/docs/structured-output) supports schema-based responses, but schema conformance alone does not verify an engineering claim or source quotation.

## UX decisions for Norte

1. **Automatic conception initialization:** `Start conception` reads linked Project Memory artifacts, extracts architecture plus requirements, saves the result, and opens System. Show an honest indeterminate activity state; failure offers retry or a return to Memory, never a fabricated model.
2. **System → Timeline → Discovery BETA:** first entry answers what Norte understood. Persist the last workspace for later visits. The ordering is an application of the shared-model review pattern, not a claim that another tool prescribes these tabs.
3. **Project-owned progression:** persist unlocked phase separately from current route, together with baseline/memory revision metadata. A memory update may indicate staleness but must not silently rebuild the model.
4. **One project selection:** the project's team is read-only context. This removes a redundant selection and prevents disagreement between project and team state.
5. **Macro architecture with explicit drill-down:** reuse Dagre and existing canvas navigation. Keep requirements and most properties outside normal graph nodes.
6. **Intentional details:** selection highlights; the explicit information affordance opens a compact dialog. Do not reserve a permanent details column.
7. **Requirements overlay:** a small count opens a searchable list; one filter surface contains editable, generic facets. Selecting a requirement highlights linked entities/relations, including those affected in a scenario. Preserve original source provenance after edits.
8. **Quiet contextual Discovery:** keep free writing, movement, pan/zoom/fit and history. Replace global organization and change buttons with the contextual technical hypothesis action chosen above.
9. **One shared scenario engine:** preview the resolved target and new value; then reveal temporary causal paths. Clearing restores the workspace. Explicitly saved scenarios persist separately from the baseline.
10. **Auditable conclusions:** critical requires supported facts and a concrete rule/comparison. Missing information remains review; checked compatibility can be valid. Expose inputs, normalized units, rule, calculation, traversed relation IDs and citations. AI inference adds short justification, confidence, model and time, never hidden chain-of-thought.

Validation should measure task completion time, dependency recall, critical-impact precision, unsupported-claim rate and traceability coverage. These are planned metrics; this research did not measure them.
